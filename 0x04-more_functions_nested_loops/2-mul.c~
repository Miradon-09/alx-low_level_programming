#include "main.h"
/**
 * mul - multiply 2 numbers  
 *@b - int number
 *@c - multi[ly int a and b
 *@a - int number
 *
 * Return: int var c
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);

	return (c);
}
