#include "main.h"
/**
 * print_most_numbers - function that prints number from 0 to 9
 * except 2 anmd 4 
 *@a - int 0 to 9
 * _putchar - print int a value  
 * Return: Always 0, success
 */
void print_most_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		if (a != '2' && a != '4')
			_putchar(a);
	}
	_putchar('\n');
}
